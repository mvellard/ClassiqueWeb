@model IEnumerable<ClassiqueWeb.Models.Instrumentation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instrument.Nom_Instrument)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oeuvre.Titre_Oeuvre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instrument.Nom_Instrument)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oeuvre.Titre_Oeuvre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Code_Instrumentation }) |
            @Html.ActionLink("Details", "Details", new { id=item.Code_Instrumentation }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Code_Instrumentation })
        </td>
    </tr>
}

</table>
