@model IEnumerable<ClassiqueWeb.Models.Instrument>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom_Instrument)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Image)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_Instrument)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Image)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Code_Instrument }) |
            @Html.ActionLink("Details", "Details", new { id=item.Code_Instrument }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Code_Instrument })
        </td>
    </tr>
}

</table>
