@model IEnumerable<ClassiqueWeb.Models.Composition_Oeuvre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Composition.Titre_Composition)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oeuvre.Titre_Oeuvre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Composition.Titre_Composition)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oeuvre.Titre_Oeuvre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Code_Composer_Oeuvre }) |
            @Html.ActionLink("Details", "Details", new { id=item.Code_Composer_Oeuvre }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Code_Composer_Oeuvre })
        </td>
    </tr>
}

</table>
